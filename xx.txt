原在homeMina 点击按钮切换场景，原广告是没有被销毁的，因为点击返回到main界面，广告依旧在

请求好的数据存在数组里好还是哪里好？
拿数据怎么拿好？
数据拿的太慢了<怎么保证有广告数据才创建实例？

如何保证池子里面始终有一个广告可以使用？


如何监听某个数组里面有数据！


关闭销毁广告对象

nativeAd.showDownloadButton(Object object)（1115+）
描述
显示控件按钮。


整个页面进行上报
nativeAd.reportAdClick


原生广告：load 方法全局仅能有效调用一次（重复调用会失效，甚至触发报错）；
激励视频广告：load 方法支持多次调用（但需遵循 “用完即加载” 的规则，且同一时间仅能有一个加载中的实例）。