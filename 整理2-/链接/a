图片转换 https://convert.io/cn/image-converter

图片压缩 https://tinypng.com/

torotisegit    乌龟

插件

hap extension 快应用高亮
settings cycler 快捷命令行
turbo console log 打印

oppo
https://ie-activity-cn.heytapimage.com/static/minigame/CN/docs/index.html#/develop/games/use

https://ie-activity-cn.heytapimage.com/static/minigame/CN/docs/index.html#/develop/ad/video-ad

https://open.oppomobile.com/documentation/page/info?id=11871


huwei：
https://developer.huawei.com/consumer/cn/doc/games-guides/games-quickgame-tool-download-0000002351893589

面试；
https://pinkprisma.com/main/interview-question/hot

快应用
https://nice.lovejade.cn/zh/article/develop-quick-app-experience-notes.html#%E5%BF%AB%E5%BA%94%E7%94%A8%E7%A4%BA%E4%BE%8B


原在homeMina 点击按钮切换场景，原广告是没有被销毁的，因为点击返回到main界面，广告依旧在

请求好的数据存在数组里好还是哪里好？
拿数据怎么拿好？
数据拿的太慢了<怎么保证有广告数据才创建实例？

如何保证池子里面始终有一个广告可以使用？


如何监听某个数组里面有数据！


关闭销毁广告对象

nativeAd.showDownloadButton(Object object)（1115+）
描述
显示控件按钮。


整个页面进行上报
nativeAd.reportAdClick


原生广告：load 方法全局仅能有效调用一次（重复调用会失效，甚至触发报错）；
激励视频广告：load 方法支持多次调用（但需遵循 “用完即加载” 的规则，且同一时间仅能有一个加载中的实例）。

 MynativeAd.onLoad(function (test:any) {
 MynativeAd是事件对象，，test是触发回调后的对象包含取消  MynativeAd的adid，，，区分


「
	点击上报完视频后也要可以销毁，
	销毁广告对象后，按钮ui只是隐藏，那么adid如何对象上新的广告对象呢？

」
横屏宽是高，高是宽
要先乘以设备像素比







在鸿蒙（HarmonyOS）开发中，销毁广告对象（调用 destroy()）不会自动销毁通过 showDownloadButton生成的按钮。以下是关键分析：

🔍 核心原因

广告对象与UI组件解耦
广告对象（如 rewardedVideoAd）的生命周期管理与UI组件（如按钮）相互独立：

广告对象通过 destroy()仅释放广告资源（如监听器、广告实例），见示例代码：
rewardedVideoAd.offError(); // 注销错误监听
rewardedVideoAd.offLoad();  // 注销加载监听
rewardedVideoAd.destroy();  // 销毁广告实例
UI按钮由开发者显式创建（如 Button组件），需在页面或组件生命周期中手动销毁。
按钮需手动管理
showDownloadButton生成的按钮属于自定义UI组件，其生命周期需通过以下方式控制：

在页面销毁（onPageHide或 onDestroy）时主动移除组件。
使用条件渲染（如 if语句）动态控制按钮显隐：
@State isButtonVisible: boolean = true;

build() {
  Column() {
    if (this.isButtonVisible) {
      Button('下载')
        .onClick(() => { /* ... */ })
    }
  }
}
在广告销毁后，通过 this.isButtonVisible = false触发按钮销毁。
⚠️ 注意事项
内存泄漏风险
若广告销毁后未主动清理按钮，残留的按钮可能持续占用内存（如事件监听未注销）。
最佳实践
在广告对象的 onClose或 onDestroy回调中同步更新UI状态：